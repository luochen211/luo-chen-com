<view class="custom-nav">
  <button wx:if="{{back}}" class="custom-nav__back" bindtap="goBack" aria-label="返回">‹</button>
  <text class="custom-nav__title">{{title}}</text>
  <button wx:if="{{action}}" class="custom-nav__action" bindtap="tapAction" aria-label="{{action}}">⇧</button>
</view>
